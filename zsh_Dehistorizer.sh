#!/bin/sh

currentLines=$(grep -c '^' ~/.zsh_history)
contrastor=""
searchdex=""

echo "Currently handling a grand total of: $currentLines lines. Please stand by..."
while [$currentLines - $contrastor -gt 0]
do
	
done
